# Frontend environment (Vite, build-time; only VITE_* vars are exposed).
# Point these at your backend (Render/railway/local). The backend allows
# requests from any origin (CORS is open), so this works from any host.
VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
